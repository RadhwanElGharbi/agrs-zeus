# PIRL Project Configuration
# Generated by ZEUS AGRS for test_project2

# Project Section
project:
  name: test_project2
  code: TP2_ITALY_V1
  client: SAIPEM
  crs_epsg: 32633
  measurement_units: SI

# Legacy format (for compatibility)
project_name: test_project2
project_code: TP2_ITALY_V1
client_name: SAIPEM
epsg_code: 32633
measurement_units: SI

# Start and End Points (UTM 33N coordinates)
# Start: 43.388493°N, 13.514053°E
start_x: 379647.98
start_y: 4805029.95
# End: 42.898254°N, 13.877811°E
end_x: 408381.01
end_y: 4750126.95

# AOI Bounds (UTM 33N)
aoi_bbox:
  min_x: 362000
  min_y: 4745000
  max_x: 425000
  max_y: 4815000

# Cost Weights (Enhanced - includes hydraulics and regulatory)
cost_weights:
  terrain_difficulty: 0.20      # Reduced from 0.30
  water_crossings: 0.15          # Reduced from 0.20
  infrastructure_crossings: 0.10 # Reduced from 0.15
  environmental_impact: 0.12     # Reduced from 0.15
  row_acquisition: 0.08          # Reduced from 0.10
  permitting_complexity: 0.08    # Reduced from 0.10
  hydraulic_costs: 0.12          # NEW - pumping stations, flow optimization
  regulatory_penalties: 0.15     # NEW - NTC 2018, Natura 2000 violations

# Hydraulics Configuration
hydraulics:
  flow_rate_m3_s: 0.5            # Volumetric flow rate for 26" gas pipeline
  fluid_type: "natural_gas"      # Gas, Oil, Water, H2, CO2, NGL
  temperature_k: 288.15          # 15°C operating temperature
  enable_pumping_stations: true  # Allow automatic pumping station placement
  max_pressure_drop_mpa: 5.0     # Maximum pressure drop before pump required

# Regulatory Compliance Configuration
regulatory:
  country_code: "ITA"            # Italy
  region: "Marche-Umbria"        # Specific region for threshold loading
  enable_cost_penalties: true    # Apply regulatory violation costs
  load_from_docs: true           # Load thresholds from regulatory_docs/

# Constraints Section
constraints:
  max_slope_percent: 20  # SAIPEM requirement (stricter than industry standard 30%)
  max_curvature_rad_per_m: 0.01
  min_crossing_angle_deg: 75  # SAIPEM prefers near-orthogonal crossings (target 90°)
  buffer_protected_areas_m: 100
  buffer_water_bodies_m: 50
  max_segment_length_m: 100
  allowed_hot_bend_angles_deg: [15, 30, 45, 60, 90]  # SAIPEM hot bend angles

# Legacy format (for compatibility)
max_slope_percent: 20
max_curvature_rad_per_m: 0.01
min_crossing_angle_deg: 75
buffer_protected_areas_m: 100
buffer_water_bodies_m: 50
max_segment_length_m: 100
allowed_hot_bend_angles_deg: [15, 30, 45, 60, 90]

# Training Section
training:
  total_timesteps: 100000  # 100k timesteps for validation run
  num_envs: 8  # Parallel environments
  eval_freq: 5000  # Evaluate every 5k steps
  save_freq: 25000  # Save model every 25k steps
  max_steps_per_episode: 5000
  learning_rate: 0.0003
  batch_size: 256
  algorithm: PPO  # Proximal Policy Optimization
  # PPO-specific parameters
  n_steps: 2048  # Steps per rollout
  gamma: 0.99  # Discount factor
  gae_lambda: 0.95  # GAE parameter
  clip_range: 0.2  # PPO clip range
  ent_coef: 0.01  # Entropy coefficient
  vf_coef: 0.5  # Value function coefficient
  max_grad_norm: 0.5  # Gradient clipping

# Legacy format (for compatibility)
total_timesteps: 100000
num_envs: 8
eval_freq: 5000
save_freq: 25000
max_steps_per_episode: 5000
learning_rate: 0.0003
batch_size: 256
algorithm: PPO
n_steps: 2048
gamma: 0.99
gae_lambda: 0.95
clip_range: 0.2
ent_coef: 0.01
vf_coef: 0.5
max_grad_norm: 0.5

# Paths
project_dir: /opt/agrs/Projects/test_project2
data_dir: /opt/agrs/Projects/test_project2/data
output_dir: /opt/agrs/Projects/test_project2/PIRL/outputs/pirl_training_100k
tensorboard_log: /opt/agrs/Projects/test_project2/PIRL/outputs/pirl_training_100k/tensorboard
model_save_path: /opt/agrs/Projects/test_project2/PIRL/models/pirl_italy_100k


