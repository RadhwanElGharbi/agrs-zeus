[pytest]
testpaths = tests
python_files = test_*.py
python_classes = Test*
python_functions = test_*
asyncio_mode = auto

# Custom markers
markers =
    regression: Regression tests that must always pass
    smoke: Quick smoke tests for pre-demo validation
    integration: Integration tests that make real API calls
    slow: Slow-running tests
