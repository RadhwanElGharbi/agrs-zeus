# Analytics (local runtime data)

This folder is **intentionally not committed** to GitHub.

It is used for **local runtime outputs** (event/session logs, contact leads exports, caches).

If you need analytics in production, configure ZEUS to write these outputs to an external
storage location (e.g., object storage) and keep this repository **code-only**.





